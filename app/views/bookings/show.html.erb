<style>
/* Styling for booking action buttons - Unique class names */
.booking-actions-param {
  text-align: center;
  margin-top: 20px;
}

/* Edit button */
.btn-edit-param {
  color: white;
  background-color: #007bff;
  padding: 10px 15px;
  text-decoration: none;
  border-radius: 5px;
  font-weight: bold;
  transition: background 0.3s ease-in-out;
}

.btn-edit-param:hover {
  background-color: #0056b3;
}

/* Back button */
.btn-back-param {
  color: white;
  background-color: #6c757d;
  padding: 10px 15px;
  text-decoration: none;
  border-radius: 5px;
  font-weight: bold;
  transition: background 0.3s ease-in-out;
}

.btn-back-param:hover {
  background-color: #5a6268;
}

/* Cancel button */
.btn-cancel-param {
  display: block;
  width: 100%;
  max-width: 300px;
  margin: 20px auto;
  background-color: #dc3545;
  color: white;
  padding: 12px;
  text-align: center;
  font-size: 16px;
  font-weight: bold;
  border: none;
  cursor: pointer;
  border-radius: 5px;
  transition: background 0.3s ease-in-out;
}

.btn-cancel-param:hover {
  background-color: #c82333;
}
</style>

<p style="color: green"><%= notice %></p>
<%= render @booking %>
<div class="booking-actions-param">
<%= link_to "Back to bookings", bookings_path, class: "btn-back-param" %>
<%= button_to "Cancel this booking", @booking, method: :delete, data: { confirm: "Are you sure?" }, class: "btn-cancel-param" %>
</div>



